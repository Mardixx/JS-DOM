/*Selectors*/
/* let newTitle = document.querySelectorAll('.important')

for (child of newTitle) {
    child.title = 'This is an important item'
} */


/* let getImg = document.getElementsByTagName('img'); 


for (child of getImg) {
    if (child.getAttribute("class")) {
        child.style.display = 'block';
    }
    else {
        child.style.display = 'none';
    }
} */

/* let getP = document.getElementsByTagName('p');

for (child of getP) {
    console.log(child.textContent);
    if (child.getAttribute('class')) {
        console.log(child);
    }
} */


/* let red = Math.floor(Math.random()* 256)
let green = Math.floor(Math.random()* 256)
let blue = Math.floor(Math.random()* 256)

let randomColor = `rgb(${red}, ${green}, ${blue})`;

let getP = document.getElementsByTagName('p');

for (child of getP) {
    if (child.getAttribute('class')) {
        child.style.color = 'black'
    }
    else {
        child.style.color = randomColor
    }
}
 */
